// Next.js loading.tsx file - automatically displays while page.tsx is loading
// This is a special file name that Next.js recognizes and uses automatically

import Loading from "./components/Loading";

export default function AppLoading() {
  return <Loading />;
}
